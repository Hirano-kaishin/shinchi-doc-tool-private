HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=1

# Set 1 to enable Claude call in AgentService.
ENABLE_REMOTE_LLM=0
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-4-8

# Keep * for local dev. For production, set your frontend origin.
FRONTEND_ORIGIN=*
